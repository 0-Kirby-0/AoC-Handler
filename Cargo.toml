[package]
name = "aoc_handler"
version = "0.1.0"
edition = "2021"

[dependencies]
derive_more = {version = "1.0.0", features = ["from"]}
dirs = "5.0.1"
reqwest = {version = "0.12.9", features = ["blocking"]}
